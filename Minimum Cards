#include<stdio.h>

int main(){
    int n, count=0, i;
    char str[50];
    scanf("%d %s", &n, str);
    for(i=0; i<n; i++){
        if(str[i]== str[i-1]){
            count++;
    }
    }

    printf("%d", count);
    return 0;
}
